export enum TScheduleBookingStatus {
  pending = 'pending',
  scheduled = 'scheduled',
  completed = 'completed',
  cancelled = 'cancelled',
}

export enum TPaymentStatus {
     unpaid = 'unpaid',
     paid = 'paid',
     refunded = 'refunded',
     failed = 'failed',
}