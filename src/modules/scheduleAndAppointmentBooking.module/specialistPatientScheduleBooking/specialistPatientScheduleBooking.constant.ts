export enum TScheduleBookingStatus {
  scheduled = 'scheduled',
  completed = 'completed',
  cancelled = 'cancelled',
}

export enum TPaymentStatus {
     UNPAID = 'unpaid',
     PAID = 'paid',
     REFUNDED = 'refunded',
     CANCELLED = 'cancelled',
}